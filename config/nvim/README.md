- Copy this folder to `~/.config/nvim`
- Make sure to run the following commands/install the following packages some other way
```
npm install tree-sitter-cli
apt install unzip
apt install python3.12-venv
```
