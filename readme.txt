clone this dotfiles repo into your home directory. then run config_symlinker.sh and enter whichever directories/files you want to create symlinks for. 

also run `git submodule update --init --recursive` to update any submodules inside the repo
